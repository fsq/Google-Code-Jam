#include <vector>
#include <algorithm>
#include <iostream>
#define REP(i,a,b) for (int i=a; i<=b; ++i)

using namespace std;

int n, q;
int a[103], b[103], c[103];

int main() {
	ios::sync_with_stdio(false);
	int T;
	cin >> T:
	REP(t, 1, T) {
		cout << "Case #" << t << ": ";
		cin >> n >> q;

		if (n==1)

	}

	return 0;
}